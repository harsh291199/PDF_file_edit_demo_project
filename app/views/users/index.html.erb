<h1>Users</h1>

<% @users.each do |user| %>
  <p> First Name : <%= user.fname %></p>
  <p> Business Name : <%= user.b_name %></p>
  <%= link_to "show", user_path(user) %>
  <%= link_to "edit", edit_user_path(user) %>
  <%= link_to "delete", user_path(user), method: :delete %>
<% end %>